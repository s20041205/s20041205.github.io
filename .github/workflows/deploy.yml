# Disabled: publish_branch: main would push config.js (with real URLs) back to
# the repo, defeating the purpose of keeping URLs out of git history.
# To re-enable properly, configure GitHub Pages to serve from a gh-pages branch.
name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow is disabled. See comments at top of file."
